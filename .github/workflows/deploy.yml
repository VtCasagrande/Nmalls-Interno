name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  dummy-success:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Dummy success step
        run: echo "Este é um workflow de placeholder. O deploy real é feito pelo EasyPanel."
